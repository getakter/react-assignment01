import React from 'react';
import logo from './../../images/logo.png'
import './Header.css'

const Header = (props) => {

    return (
            <div id="header">
                    <img src={logo} alt=""/> 

        </div>
    );
};

export default Header;